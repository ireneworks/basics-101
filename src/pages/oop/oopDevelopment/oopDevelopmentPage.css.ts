import { style } from "@vanilla-extract/css";
import { root } from "../../../common.css.ts";

export const headingBlue = style({
  color: root.color.blue600,
  marginRight: 12,
});

export const descWrapper = style({
  marginBottom: 40,
});
